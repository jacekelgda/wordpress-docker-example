machine:
  python:
    version: 2.7.3
  php:
    version: 5.5.11
  services:
    - docker

dependencies:
  pre:
    - echo 'pre'
  post:
    - echo 'post'

test:
  override:
    - docker run jacekelgda/wordpress-docker-example:latest /var/www/bin/behat /var/www/features --config /var/www/config/behat.yml